<h1>Places</h1>

<% if @places.any? %>
  <ul class="list-group">
    <% @places.each do |place| %>
      <li class="list-group-item">
        <%= link_to place.name, place_path(place), class: 'btn btn-primary mb-2' %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No places found.</p>
<% end %>

<% if current_user %>
  <p>
    <%= link_to "New Place", new_place_path, class: 'btn btn-success' %>
  </p>
<% end %>
